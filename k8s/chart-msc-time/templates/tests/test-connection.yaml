apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "chart-msc-time.fullname" . }}-test-connection"
  labels:
    {{- include "chart-msc-time.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "chart-msc-time.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
